[project]
name = "s30"
version = "0.1.0"
requires-python = ">=3.11,<3.13"

[tool.ruff]
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]

[tool.ty.environment]
python-version = "3.11"

[dependency-groups]
dev = [
    "beautifulsoup4",
    "numpy>=2.4.4",
    "pillow>=12.2.0",
    "requests",
    "ruff>=0.15.17",
    "scikit-learn",
    "scipy>=1.17.1",
    "torch",
    "transformers",
    "ty>=0.0.49",
    "zstandard>=0.25.0",
]
